<Window x:Class="ModuKeymapStudio.AddLayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="레이어 추가" Width="430" Height="365" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{DynamicResource WindowBrush}" Foreground="{DynamicResource TextBrush}">
    <Border Padding="24" Background="{DynamicResource PanelBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Text="새 레이어" FontSize="22" FontWeight="SemiBold" />
            <StackPanel Grid.Row="1" Margin="0,20,0,0">
                <TextBlock Text="Devicetree 노드 이름" Foreground="{DynamicResource MutedBrush}" />
                <TextBox x:Name="NodeNameBox" Text="layer_2" Margin="0,6,0,0" FontFamily="Consolas" />
            </StackPanel>
            <StackPanel Grid.Row="2" Margin="0,14,0,0">
                <TextBlock Text="화면 표시 이름 (display-name)" Foreground="{DynamicResource MutedBrush}" />
                <TextBox x:Name="DisplayNameBox" Text="Layer 2" Margin="0,6,0,0" />
            </StackPanel>
            <StackPanel Grid.Row="3" Margin="0,18,0,0">
                <RadioButton x:Name="TransparentRadio" Content="모든 키를 &amp;trans로 만들기" IsChecked="True" />
                <RadioButton x:Name="CloneRadio" Content="현재 레이어 복제" Margin="0,9,0,0" />
            </StackPanel>
            <TextBlock Grid.Row="4" x:Name="ValidationText" Foreground="{DynamicResource DangerBrush}" Margin="0,14,0,0" TextWrapping="Wrap" />
            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="취소" Width="88" Margin="0,0,8,0" IsCancel="True" />
                <Button Content="추가" Width="88" Style="{StaticResource AccentButton}" Click="Add_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
